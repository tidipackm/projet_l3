{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.name }}</td>
            </tr>
            <tr>
                <th>Stade</th>
                <td>{{ equipe.stadium }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipeController_app_equipe_index') }}">Retour à la liste des équipes</a>

    <a href="{{ path('equipeController_app_equipe_edit', {'id': equipe.id}) }}">Modifier</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
