{% extends 'base.html.twig' %}

{% block title %}Championnat{% endblock %}

{% block body %}
    <h1>Championnat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ championnat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ championnat.name }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ championnat.country }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('championnatController_app_championnat_index') }}">back to list</a>

    <a href="{{ path('championnatController_app_championnat_edit', {'id': championnat.id}) }}">edit</a>

    {{ include('championnat/_delete_form.html.twig') }}
{% endblock %}
