<form method="post" action="{{ path('app_favoris_equipe_delete', {'id': favoris_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favoris_equipe.id) }}">
    <button class="btn">Supprimer</button>
</form>
