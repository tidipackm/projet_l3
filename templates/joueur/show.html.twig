{% extends 'base.html.twig' %}

{% block title %}Joueur{% endblock %}

{% block body %}
    <h1>Joueur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ joueur.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ joueur.surname }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ joueur.number }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ joueur.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('joueurController_app_joueur_index') }}">back to list</a>

    <a href="{{ path('joueurController_app_joueur_edit', {'id': joueur.id}) }}">edit</a>

    {{ include('joueur/_delete_form.html.twig') }}
{% endblock %}
